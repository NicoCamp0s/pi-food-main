

const inicialState = {
    allRecipes: [],
    allDiets: [],
    createRecipe: [],
    recipeDetail: [],
    currentPage: []
}

const reducer = (state = inicialState, action) => {
    switch(action.type) {

        

        default: 
        return {...state};
    }
}

export default reducer;